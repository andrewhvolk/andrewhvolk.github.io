# Vendored Presentation Runtime

- Reveal.js 4.6.1
- MathJax 3.2.2

These files are committed so every lecture works from disk without a network
connection. Update them only through the pinned versions in `package.json`, then
rerun the complete structural and browser QA suites.
